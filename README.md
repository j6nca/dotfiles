# dotfiles

This project leverages chezmoi to sync peronalized configuration between machines.

## Setup

```
# install chezmoi
brew install chezmoi
# apply configs
chezmoi init --apply j6nca
# install mise
brew install mise
# install everything via mise
mise install
```